export const metadata = {
  title: {
    default: "Niet Alleen",
    template: "%s — Niet Alleen",
  },
};

export default function NietAlleenLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
